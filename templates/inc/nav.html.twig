<nav>
    <ul>
        <li><a href="{{ path("main_index") }}" title="Home">Home</a></li>
        <li><a href="{{ path("serie_list") }}" title="All series">Series</a></li>
        {% if app.user %}
            <li><a href="{{ path("serie_create") }}" title="Create a new serie">Create</a></li>
            <li>Bienvenue {{ app.user.email }}</li>
        {% endif %}
    </ul>
</nav>